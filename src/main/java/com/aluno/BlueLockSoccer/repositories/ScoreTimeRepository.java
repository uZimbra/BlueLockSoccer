package com.aluno.BlueLockSoccer.repositories;

import com.aluno.BlueLockSoccer.models.ScoreTime;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScoreTimeRepository extends JpaRepository<ScoreTime, Integer> {
}
